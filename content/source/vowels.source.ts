import type { LetterDraft } from './types';

/**
 * Intentionally empty seed for prototype v0.
 * Vowels will be added after the first consonant-learning flow is working.
 */
export const vowelSource: LetterDraft[] = [];
